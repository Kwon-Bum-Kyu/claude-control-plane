# CCP 하네스 변경 이력

> CLAUDE.md 에서 분리된 하네스 개발 변경 이력 로그 — 하네스 설정 디렉터리(`.claude/`)에 보관한다. 2026-09-05 레포 단일화 이후 본 디렉터리는 공개 레포에 포함되지만, 룰 `.claude/rules/no-internal-tracking-ids.md` §3.2 적용 제외 대상이므로 작업자 추적 ID 를 계속 자유롭게 사용한다 (ID 노출은 사용자 감수 결정, 룰 §6 2026-09-05 행 참조).
>
> 신규 작업 완료 시 본 파일 하단에 행을 추가한다. CLAUDE.md 본문에는 변경 이력을 누적하지 않는다.
>
> **아카이브:** 항목이 50행에 근접하면 분기 단위로 `.claude/archive/HARNESS_CHANGELOG_{YYYY}-Q{N}.md` 로 분리한다. 지난 분기 이력은 다음 파일에 있다.
> - [2026-Q2 (4~6월, 34건)](archive/HARNESS_CHANGELOG_2026-Q2.md)

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-09 | 모델 적합성 검사 + 모델 하향 5건 적용 | `.claude/agents/{adapter-engineer,plugin-scaffolder,harness-qa,ux-designer}.md` opus→sonnet, `.claude/agents/scope-guard.md` opus→haiku, `.claude/skills/ccp-orchestrator/SKILL.md` Agent 호출 5건 동기화 (spec-writer·architecture-reviewer·token-economist·license-auditor 4건은 opus 유지) | 전역 하네스 스킬(`~/.claude/skills/harness`) 모델 정책 변동 반영 — 구버전 "전원 opus 고정" → 신버전 작업 특성별 매트릭스(opus=기획·최종검수·복잡추론 / sonnet=구현·중간검증 / haiku=분류·경량QA). scope-guard 의 haiku 하향은 CCP 플러그인 router 에이전트 "Why Haiku" 선례(결정적 판정 패턴엔 Haiku 충분)와 일치. 매트릭스의 Claude 5(fable) 미반영은 전역 스킬 후속 갱신 후보로 기록. |
| 2026-08-09 | 파이프라인 8단계 재구성 (자연어 접수→기획→검수→휴먼 승인 G1→개발→QA→휴먼 승인 G2→위키화) + 실행 모드 하이브리드 서브 에이전트 전환 | `.claude/skills/ccp-orchestrator/SKILL.md` 전면 재작성 (게이트 파일 `03_gate1_approval.md`/`06_gate2_approval.md` 신설, 산출물 번호 00~07 재편, 게이트 재개·부분 재실행 흐름 추가), `.claude/agents/harness-qa.md` 재작성 (incremental+전체 스위트 이중 국면, `memory` 활성, 출력 03_→05_), 나머지 에이전트 8건 팀 통신 프로토콜 → 서브 모드 협업 프로토콜 전환 (`spec-writer`·`scope-guard`·`ux-designer`·`architecture-reviewer`·`token-economist`·`license-auditor`·`plugin-scaffolder`·`adapter-engineer`, scaffolder/adapter 진행 보고 03_→04_), `CLAUDE.md` 하네스 포인터 갱신 (+changelog 위치 명시) | 사용자 지시 — 개발 진입(G1)·위키화 진입(G2)에 휴먼 승인 게이트 필수화, QA 를 독립 단계로 분리 (개발 내 incremental QA 는 유지), 종착지를 claude-obsidian wiki vault 위키화로 확장. 팀 옵트인 환경변수 미활성 실측 → 기존 명시적 TeamCreate 기반 팀 모드를 하이브리드 서브 에이전트 (직렬 파이프라인 + 병렬 팬아웃 + 재개) 로 전환, 에이전트 간 직접 통신은 파일 인계 + 오케스트레이터 중재로 대체. 위키화는 신규 에이전트 없이 claude-obsidian:wiki-ingest 재사용 (중복 방지). |
| 2026-08-12 | ✅ Companion 코어 통합 런 완결 (8단계 파이프라인 최초 완주 — G1·G2 승인 + 위키화) | `plugins/ccp/scripts/{companion.mjs, core/ 8모듈, adapters/ 2종}` 신규 + 기존 companion 2종 thin alias 전환 (하이브리드), `lib/codex-*.mjs` 5파일 삭제, `tests/companion/` 골든 하니스(29 시나리오)+mock 계약 테스트 신규, CI `companion-contract` 잡 신설, attribution 6파일 복원, 커맨드 8종·에이전트 2종·README·skills 문서 동기화, `_workspace/00~07` 전 산출물 + 게이트 2건 approved | 계약 SSOT = `01_schema.md` §2.2·§2.5 (52필드 동결 + unknown-key 거부). QA 조건부 합격 (PASS 16 / FAIL 2 → AC-3 LOC 2,483·AC-4 어댑터 428줄 백로그 이관, G2 사용자 확정). 골든 29/29 diff 0·회귀 3종 전건 PASS·mock 확장 증명 코어 diff 0줄. 검수 BLOCKER 7건(attribution 소멸 위기·rescue-finalize 제3 writer 등) + QA BLOCKER 1건(antigravity 고아 정리 사망) 전부 해소. 위키화: vault `.raw/` R-20260812 3종 ingest. |
| 2026-08-24 | CCP v0.3.0 실환경 E2E 테스트 런 완결 — 별도 하니스+실설치+슬래시 경로 47콜/$6.58. 런 판정 PASS, 결함 14건(S1×2: antigravity headless·고아 정리 훅) 백로그 이관, 네임스페이스 `/ccp:*` 단일 확정, 직전 런 측정불가 해소. wiki 8생성·11갱신+인덱스 3종. G1/G2 승인, teardown 원복 실측. |
| 2026-08-25 | E2E 발견 결함 수정 런 완결 (8단계 파이프라인 2회차 완주 — G1·G2 승인 + 위키화) — F-1(요약 임계 abort → 문장 경계 절단 후 성공, `summary_truncated` 참일 때만 방출, result 파일의 요약본 기록 문제 동반 해소)·F-2(비대화형 자동 승인 플래그 + opt-out, 실환경 1콜로 D-010 해소 확인)·F-3(job 경로 기준을 프로젝트 루트로 단일화, `CLAUDE_PLUGIN_ROOT` 배제로 D-006·D-012 근본 해소)·F-4(네임스페이스 31파일 159건 정정, 라우터 사용자 명시 축 잠복 결함 동반 수정). 변경 50파일, 커밋 보류(사용자 결정). QA 판정 합격 — AC 39항목 중 37 PASS·미실행 2·FAIL 0. 회귀: 골든 29/29 diff=0(기준선 8건 갱신)·계약 15/15 52키 유지·신규 절단 하니스 30/30·라우터 72케이스 오분류 0건(합격 기준을 백분율에서 오분류 0건으로 상향)·훅 19/19. G1 결정: 범위는 필수 동반 수정까지(테스트 하니스 6파일 승인), 보안 고지는 커맨드 문서 2곳, F-1 검증은 신규 하니스. G2 결정: 위키화 승인 + F-2 실환경 1콜 + MEDIUM 3건 즉시 수정 + 커밋 보류. 부수 발견: 라우터 회귀 기준이 실제 오분류를 은폐하던 구조, 유령 에러 코드 3곳, 도달 불가능한 안전 지침(호환성 표 오기). 다음 런 인계: 미실행 검증 2건(git 원격 설치·입력 릴레이), 인접 오기 4종(감사 커맨드 표기 24건·취소 커맨드 안내·CHANGELOG 신설·감사 스크립트 경로 재구현), 위임 CLI 의 셸 작업 디렉터리 불일치 정황. |
| 2026-08-27 | 전역 하네스 스킬 변경분 반영 — 캐시 리듬 규칙 채택 + frontmatter 아키타입 정비 + changelog 분기 아카이브 분리 | `.claude/skills/ccp-orchestrator/SKILL.md` (캐시 리듬 절 신설 5규칙, S5·S6 완주형 스폰 재설계, 실행 모드 표·데이터 흐름·에러 핸들링·테스트 시나리오 정합화), `.claude/agents/*.md` 9건 (`tools` allowlist 신설, `## 실행 리듬` 절 신설, `skills` preload 7건, harness-qa 장기 재개 설계 → memory 기반 완주형 전환), `.claude/skills/*/SKILL.md` 12건 (`user-invocable: false` 11건 / 오케스트레이터는 자동 트리거·사용자 호출 명시), `.claude/archive/HARNESS_CHANGELOG_2026-Q2.md` 신규 (Q2 34건 이관, 본 파일 47→16행) | 전역 하네스 스킬이 2026-08-24 에 캐시 리듬 규칙을 도입했으나 CCP 산출물은 2026-08-09 기준으로 정체되어 있었다. 규칙의 근거가 된 실측 진단에서 본 프로젝트가 블로킹 Bash 지배 패턴(콜드 28건 7.18M, 회피 가능 15건 3.31M)으로 지목된 당사자였다. 핵심 결함 2건: ① harness-qa 를 스캐폴드 시점에 스폰해 개발 사이클 전체와 전체 QA 까지 대기시키는 설계가 5분 TTL 초과를 구조적으로 확정 ② 외부 CLI·테스트 스위트·회귀 하니스에 background 지시 부재. 부수 확인: `paths` frontmatter 는 트리거를 보강하지 않고 해당 경로 작업 시로 한정하는 방향으로 작동하여, 명세 작성 단계에서 템플릿 스킬이 비활성화되는 역효과가 실측되었다. 5건 지정 후 철회하고 에이전트 `skills` preload 로 대체했다. 구조 검증 112항목 전건 통과. |
| 2026-08-30 | ✅ 서브에이전트 출구 측 회귀 기준 신설 런 완결 (8단계 파이프라인 3회차 완주 — G1·G2 승인 + 위키화). 백로그 항목 6. | `tests/subagent/{rescue-isolation-test.mjs (772줄), lib/render.mjs (262줄), rescue-isolation-fixture-test.mjs (239줄), fixtures/ (40 디렉터리 + expected.json 42케이스)}` 신규, `tests/README.md` 신설, `.github/workflows/ci.yml` companion-contract 잡에 스텝 1개(+3행), `.github/PULL_REQUEST_TEMPLATE.md` +1행, `.claude/rules/no-internal-tracking-ids.md` §3.1 에 `tests/**` 추가(G1 승인). `plugins/ccp/**` diff 0. `_workspace/00~07` 전 산출물 + 게이트 2건 approved. 직전 중단 런은 `_workspace_archive_20260830_170753/` 로 이관. 커밋 보류 | 2026-08-26 실측(codex-rescue 서브에이전트가 companion 외 `cat` 2회 실행, 메인 측 회귀 기준은 전부 통과)을 잡는 "출구 측" 검사. 검수 BLOCKER 2(실세션 PASS 도달 불가·마스킹이 `&&` 삼킴)를 오케스트레이터가 실측 재현 후 S2 복귀, 스캐너를 좌→우 단일 상태 스캔으로 교체(오케스트레이터 배터리 25/25 직접 실행), 익명화를 허용 키 화이트리스트로 전환(실측 `toolUseResult.stdout` 최대 10,777자 누출 위험). 재검수 HIGH 1(비인용 heredoc)·개발 HIGH 1(읽기 권한 오류가 `--json` 계약 위반) 즉시 해소. QA 합격 AC 12/12, 기존 CI 4종 기준선 완전 일치, Node 20/22 동일, 판정 0.23초. G1 결정: 층 B 는 기존 세션만(쿼터 0), 룰에 `tests/**` 추가. G2 결정: 위키화 승인 + 미추적 `AGENTS.md` 삭제. 위키: 10생성·11갱신. 인계: background 경로 미관측, fixture 불가 SKIP 3종, 라이선스 C1/C2 스크립트 한계, `ci.yml:134` runtime 경로 룰 예외, docs §7 출구 측 반영 별도 런, E2E 잔여 결함 5건·AC-3/4 백로그 미등재 상태 지속. |
| 2026-09-05 | 전역 dev-pipeline 하네스 채택 — 프로젝트 전용 오케스트레이터 체계 폐지 | `.claude/skills/ccp-orchestrator/` 삭제, `.claude/agents/` 9종 전체 삭제 (adapter-engineer·architecture-reviewer·harness-qa·license-auditor·plugin-scaffolder·scope-guard·spec-writer·token-economist·ux-designer), `.claude/agent-memory/harness-qa/` 11파일 삭제, `CLAUDE.md` 하네스 절을 전역 dev-pipeline 포인터로 교체. 도메인 지식 스킬 11종은 파일 그대로 유지 (에이전트 브리핑 참고 문서 용도) | 사용자 결정 (2026-09-05): 전역 하네스와의 이중 유지비 제거 (모델 매트릭스·캐시 리듬 등 전역 변경분을 프로젝트판에 수동 동기화해온 이력 2회). 검수 3축 (아키텍처·토큰·라이선스 병렬 검수) 은 대응 단계 없이 제거 확정, harness-qa agent-memory 는 이관 없이 삭제 확정. 도메인 스킬 6종에 구 에이전트 참조가 잔존하나 파일 유지 결정에 따라 미수정 (후속 정리 후보). 진행 중 런 없음 (직전 런 2026-08-31 커밋 완료) 을 확인 후 전환. |